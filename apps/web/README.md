# apps/web

KEOM frontend — Next.js (App Router), TypeScript, Tailwind CSS.

See the root [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md) for the full architecture blueprint.

## Development

From the repo root:

```bash
pnpm install
pnpm --filter @keom/web dev
```
